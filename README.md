# Temporary download branch

This is an **orphan branch** holding a single artifact for download. Delete after use.

- `k8s-masterguide.html` — Kubernetes 마스터피스 가이드 v2 Scuderia Edition (447 KB, single-file HTML, airgap-friendly)
- `k8s-masterguide.zip` — same file zipped (139 KB, ~69% smaller)

After downloading, you can delete this branch:

```
git push origin --delete tmp/k8s-download
```
